{
  "name": "darknorev4-main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "https://milestones95.github.io/darknorev4",
  "scripts": {
    "predeploy": "cd client && npm run build",
   "deploy": "gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1",
   "start": "cd server && npm install && node index.js",
    "build": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^5.0.0",
    "react-router-dom": "^6.14.0"
  },
  "devDependencies": {
    "eslint": "^8.43.0",
    "eslint-plugin-react": "^7.32.2"
  }
}
